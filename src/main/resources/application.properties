spring.application.name=sphere-d

server.port=5001

spring.datasource.url=jdbc:postgresql://localhost:5432/d_sphere_db
spring.datasource.username=vaibhav.singhal
spring.datasource.password=psql

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# JWT (HS256 signing key: at least 32 UTF-8 characters)
app.jwt.secret=7y5g2UGcSrkH5VFEQwa6VehvQBey2miL44dWk7G
app.jwt.expiration-ms=86400000
app.jwt.cookie-name=jwt

# CORS (comma-separated; required when using credentials + cookies from browsers)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5001

app.system.email=system@sphere-d.com
app.system.password=SystemSecret!@123